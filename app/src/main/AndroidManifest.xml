<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.truccongle.kiemtraiq">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<activity android:name=".MainActivity"
    android:theme="@style/Theme.AppCompat.Light.NoActionBar"
    ></activity>
        <activity android:name=".TestIQ" android:parentActivityName=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"
            />
        <activity android:name=".Data"  android:parentActivityName=".MainActivity"
            android:label="Quay lại"/>
        <activity android:name=".LichSu"  android:parentActivityName=".MainActivity"
            android:label="Quay lại"/>
        <activity android:name=".ThongTinIQ"  android:parentActivityName=".MainActivity"
            android:label="Quay lại"/>
        <activity android:name=".ThongTinUngDung"  android:parentActivityName=".MainActivity"
            android:label="Quay lại"/>
        <activity android:name=".KetQua" android:parentActivityName=".MainActivity"
            android:label="Quay lại"></activity>

    </application>

</manifest>